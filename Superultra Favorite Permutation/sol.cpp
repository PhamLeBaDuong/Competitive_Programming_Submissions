#include <math.h>
#include <iostream>
#include <string>
#include <utility>
#include <vector>
#include <algorithm>
#include <unordered_map>
#include <queue>
#include <unordered_set>
using namespace std;
void bt() {
    for(auto i : st) {
        if(  || curr == 0) {

        }
    }
}
int main() {
    int t;
    cin >> t;
    while(t--) {
        int n;
        cin >> n;
        unordered_set<int> st;
        for(int i = 1; i <= n; i++) {
            st.insert(i);
        }
    }
}